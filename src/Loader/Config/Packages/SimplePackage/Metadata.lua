--// Package Metadata
return {
	Name = "SimplePackage";
	Version = 1.0;
	Description = [[ This is a simpler (barebones) version of the example package that can be used as a base for all future packages ]];
	Author = "Sceleratis";
	Package = script.Parent;
	Dependencies = {
		
	};
}