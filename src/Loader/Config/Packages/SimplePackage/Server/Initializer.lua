--[[

	Description: 
	Author: 
	Date: 

--]]


--// Initializer functions
return {
	Init = function(Root, Packages)
		--// Do server-side init stuff
		--// print("Hello World! We're in the server init!")
	end;

	AfterInit = function(Root, Packages)
		--// Do server-side stuff after init
		--// print("Hello World! We're in the server after init!")
	end;
}